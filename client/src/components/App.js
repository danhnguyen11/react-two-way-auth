import React, { Component } from 'react';
import { BrowserRouter, Route } from 'react-router-dom';

class App extends Component {
     render(){ 
      return (
        <div>
          <h1>Dan React Boilerplate</h1>
        </div>
    )
  }
}

export default App;